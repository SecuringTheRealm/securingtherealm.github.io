---
/**
 * ForgeIcon.astro
 * 8-bit pixel art forge/anvil icon
 * Follows 8px grid system for retro aesthetic
 */
---

<svg
  xmlns="http://www.w3.org/2000/svg"
  viewBox="0 0 96 96"
  width="96"
  height="96"
  role="img"
  aria-label="Forge icon - blacksmith anvil and hammer with sparks"
  class="forge-icon"
>
  <title>Forge</title>

  <!-- Anvil base/stand -->
  <rect x="16" y="64" width="48" height="8" fill="var(--colour-stone-dark)" stroke="var(--colour-gold)" stroke-width="2"/>
  <rect x="20" y="72" width="40" height="4" fill="var(--colour-stone)" stroke="var(--colour-gold)" stroke-width="2"/>

  <!-- Anvil body -->
  <rect x="24" y="48" width="32" height="16" fill="var(--colour-stone)" stroke="var(--colour-gold)" stroke-width="2"/>
  <!-- Anvil highlight (metallic shine) -->
  <rect x="48" y="50" width="6" height="4" fill="var(--colour-stone-light)" opacity="0.6"/>
  <!-- Anvil shadow -->
  <rect x="26" y="60" width="8" height="4" fill="var(--colour-ink)" opacity="0.2"/>

  <!-- Anvil horn (left pointy part) -->
  <rect x="20" y="40" width="8" height="8" fill="var(--colour-stone)" stroke="var(--colour-gold)" stroke-width="2"/>
  <rect x="16" y="36" width="8" height="4" fill="var(--colour-stone-dark)" stroke="var(--colour-gold)" stroke-width="2"/>

  <!-- Anvil pritchel hole -->
  <circle cx="36" cy="56" r="3" fill="var(--colour-ink)" opacity="0.5"/>

  <!-- Hammer head (striking the anvil) -->
  <rect x="52" y="20" width="16" height="8" fill="var(--colour-stone-dark)" stroke="var(--colour-gold)" stroke-width="2"/>
  <!-- Hammer face highlight -->
  <rect x="54" y="22" width="4" height="4" fill="var(--colour-stone-light)" opacity="0.5"/>

  <!-- Hammer handle -->
  <rect x="56" y="28" width="8" height="28" fill="var(--colour-parchment-dark)" stroke="var(--colour-gold)" stroke-width="2"/>
  <!-- Handle grip lines -->
  <line x1="56" y1="36" x2="64" y2="36" stroke="var(--colour-ink)" stroke-width="1" opacity="0.3"/>
  <line x1="56" y1="44" x2="64" y2="44" stroke="var(--colour-ink)" stroke-width="1" opacity="0.3"/>
  <line x1="56" y1="52" x2="64" y2="52" stroke="var(--colour-ink)" stroke-width="1" opacity="0.3"/>

  <!-- Hot metal being worked (on anvil) -->
  <rect x="36" y="44" width="12" height="4" fill="var(--colour-forge-orange)" stroke="var(--colour-gold)" stroke-width="1"/>

  <!-- Sparks (pixel style) -->
  <rect x="32" y="36" width="4" height="4" fill="var(--colour-forge-yellow)" opacity="0.9"/>
  <rect x="44" y="32" width="4" height="4" fill="var(--colour-forge-yellow)" opacity="0.8"/>
  <rect x="28" y="44" width="4" height="4" fill="var(--colour-forge-orange)" opacity="0.7"/>
  <rect x="50" y="38" width="4" height="4" fill="var(--colour-forge-yellow)" opacity="0.6"/>
  <rect x="40" y="28" width="4" height="4" fill="var(--colour-forge-orange)" opacity="0.5"/>
</svg>

<style>
  .forge-icon {
    shape-rendering: crispEdges;
    image-rendering: pixelated;
  }
</style>
