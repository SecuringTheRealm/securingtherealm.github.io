---
/**
 * LibraryIcon.astro
 * 8-bit pixel art library/books icon
 * Follows 8px grid system for retro aesthetic
 */
---

<svg
  xmlns="http://www.w3.org/2000/svg"
  viewBox="0 0 96 96"
  width="96"
  height="96"
  role="img"
  aria-label="Library icon - stack of ancient books"
  class="library-icon"
>
  <title>Library</title>

  <!-- Book 1 (bottom) - teal tome -->
  <rect x="8" y="56" width="64" height="16" fill="var(--colour-teal-dark)" stroke="var(--colour-gold)" stroke-width="2"/>
  <!-- Spine decoration -->
  <rect x="12" y="60" width="4" height="8" fill="var(--colour-gold)" opacity="0.4"/>
  <rect x="20" y="60" width="2" height="8" fill="var(--colour-gold)" opacity="0.3"/>
  <rect x="24" y="60" width="2" height="8" fill="var(--colour-gold)" opacity="0.3"/>
  <!-- Pages edge -->
  <rect x="72" y="58" width="4" height="12" fill="var(--colour-parchment)" opacity="0.8"/>
  <!-- Highlight -->
  <rect x="60" y="58" width="8" height="2" fill="var(--colour-teal-light)" opacity="0.5"/>

  <!-- Book 2 (middle) - stone grimoire -->
  <rect x="16" y="40" width="56" height="16" fill="var(--colour-stone)" stroke="var(--colour-gold)" stroke-width="2"/>
  <!-- Cover decoration -->
  <rect x="20" y="44" width="8" height="8" fill="var(--colour-gold)" opacity="0.4"/>
  <rect x="36" y="46" width="16" height="4" fill="var(--colour-gold)" opacity="0.3"/>
  <!-- Pages -->
  <rect x="68" y="42" width="4" height="12" fill="var(--colour-parchment)" opacity="0.8"/>
  <!-- Shadow -->
  <rect x="18" y="54" width="8" height="2" fill="var(--colour-ink)" opacity="0.2"/>

  <!-- Book 3 (top) - teal codex -->
  <rect x="24" y="24" width="48" height="16" fill="var(--colour-teal-light)" stroke="var(--colour-gold)" stroke-width="2"/>
  <!-- Cover emblem -->
  <circle cx="36" cy="32" r="4" fill="var(--colour-gold)" opacity="0.6"/>
  <rect x="44" y="30" width="2" height="4" fill="var(--colour-gold)" opacity="0.4"/>
  <rect x="48" y="30" width="2" height="4" fill="var(--colour-gold)" opacity="0.4"/>
  <!-- Pages -->
  <rect x="68" y="26" width="4" height="12" fill="var(--colour-parchment)" opacity="0.8"/>

  <!-- Open book (top) -->
  <rect x="32" y="8" width="32" height="16" fill="var(--colour-parchment)" stroke="var(--colour-gold)" stroke-width="2"/>
  <!-- Page fold -->
  <line x1="48" y1="8" x2="48" y2="24" stroke="var(--colour-stone)" stroke-width="2"/>
  <!-- Text lines (left page) -->
  <line x1="36" y1="12" x2="44" y2="12" stroke="var(--colour-ink)" stroke-width="1" opacity="0.4"/>
  <line x1="36" y1="16" x2="44" y2="16" stroke="var(--colour-ink)" stroke-width="1" opacity="0.4"/>
  <line x1="36" y1="20" x2="44" y2="20" stroke="var(--colour-ink)" stroke-width="1" opacity="0.4"/>
  <!-- Text lines (right page) -->
  <line x1="52" y1="12" x2="60" y2="12" stroke="var(--colour-ink)" stroke-width="1" opacity="0.4"/>
  <line x1="52" y1="16" x2="60" y2="16" stroke="var(--colour-ink)" stroke-width="1" opacity="0.4"/>
  <line x1="52" y1="20" x2="60" y2="20" stroke="var(--colour-ink)" stroke-width="1" opacity="0.4"/>
</svg>

<style>
  .library-icon {
    shape-rendering: crispEdges;
    image-rendering: pixelated;
  }
</style>
