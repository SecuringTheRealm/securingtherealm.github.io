---
/**
 * TowerIcon.astro
 * 8-bit pixel art castle tower icon
 */
---

<svg
  xmlns="http://www.w3.org/2000/svg"
  viewBox="0 0 80 80"
  width="80"
  height="80"
  role="img"
  aria-label="Tower icon - castle turret"
  class="tower-icon"
>
  <title>Tower</title>

  <!-- Tower base -->
  <rect x="24" y="48" width="32" height="24" fill="var(--colour-stone)" stroke="var(--colour-gold)" stroke-width="2"/>

  <!-- Tower main body -->
  <rect x="24" y="24" width="32" height="24" fill="var(--colour-stone-dark)" stroke="var(--colour-gold)" stroke-width="2"/>

  <!-- Window -->
  <rect x="36" y="32" width="8" height="12" fill="var(--colour-teal-light)" stroke="var(--colour-gold)" stroke-width="2"/>

  <!-- Battlements (top) -->
  <rect x="24" y="16" width="8" height="8" fill="var(--colour-stone)" stroke="var(--colour-gold)" stroke-width="2"/>
  <rect x="40" y="16" width="8" height="8" fill="var(--colour-stone)" stroke="var(--colour-gold)" stroke-width="2"/>
  <rect x="48" y="16" width="8" height="8" fill="var(--colour-stone)" stroke="var(--colour-gold)" stroke-width="2"/>

  <!-- Door -->
  <rect x="32" y="56" width="16" height="16" fill="var(--colour-parchment-dark)" stroke="var(--colour-gold)" stroke-width="2"/>
</svg>

<style>
  .tower-icon {
    shape-rendering: crispEdges;
    image-rendering: pixelated;
  }
</style>
